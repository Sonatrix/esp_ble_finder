#pragma once

/* Empty = hunt the loudest nearby advertisement. Or a substring, like "iPhone". */
#define TARGET_NAME ""

/* Optional stable address, e.g. "AA:BB:CC:DD:EE:FF". Empty to ignore. */
#define TARGET_ADDR ""
